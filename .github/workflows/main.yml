name: Pull Request
on:
  pull_request:
    branches:
    - master

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: testing job 😃
      uses: ./
      with:
        mattermost_webhook: ${{ secrets.MATTERMOST_WEBHOOK }}